#include"headers.h"
#include "declarations.h"


int accept(int * arr, int size)
{
    int i =0;
    printf("enter the elements of arrays :  \n");
    for (i=0;i<size;i++)
    {
    scanf("%d",arr+i);
    }

}